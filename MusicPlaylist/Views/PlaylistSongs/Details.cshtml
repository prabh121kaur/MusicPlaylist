@model CoreEntityFramework.Models.Playlist

<h2>Playlist Details</h2>

<div>
    <h4>Playlist</h4>
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Description</dd>
        <dt class="col-sm-2">Playlist Color</dt>
        <dd class="col-sm-10">@Model.PlaylistColor</dd>
        <dt class="col-sm-2">Is Private</dt>
        <dd class="col-sm-10">@Model.IsPrivate</dd>
    </dl>
</div>

<a asp-action="Edit" asp-route-id="@Model.PlaylistId">Edit</a> |
<a asp-action="Index">Back to List</a>
